{
  "version": 2,
  "installCommand": "if [ -d apps/web ]; then cd apps/web; fi; npm install --no-audit --no-fund",
  "buildCommand": "if [ -d apps/web ]; then cd apps/web && npm run build && rm -rf ../../.next && cp -R .next ../../.next; else npm run build; fi",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
