{
  "name": "piccolo-kids-migration",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @piccolo/web",
    "dev:web": "npm run dev --workspace @piccolo/web",
    "dev:api": "npm run dev --workspace @piccolo/api",
    "build": "npm run build --workspace @piccolo/web && npm run build --workspace @piccolo/api",
    "lint": "npm run lint --workspace @piccolo/web && npm run lint --workspace @piccolo/api",
    "typecheck": "npm run typecheck --workspace @piccolo/web && npm run typecheck --workspace @piccolo/api"
  },
  "engines": {
    "node": ">=20"
  }
}
